{
  "name": "marina-mcp-graph",
  "version": "1.0.0",
  "description": "MCP server for Microsoft Graph API — email, calendar. Secrets stay on the server, never exposed to the AI agent.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "keywords": ["mcp", "microsoft-graph", "email", "openclaw"],
  "license": "MIT",
  "dependencies": {
    "@azure/msal-node": "^2.16.0",
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
